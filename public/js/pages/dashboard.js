import { api } from '../api.js';import { state,unitOptions,escapeHtml } from '../state.js';import { kpi,bars,formData,$ } from '../ui.js';
export async function dashboardPage(root){root.innerHTML=`<div class="page-head"><div><h2>Centro de gestión SSOMA</h2><p>Una sola lectura para capacitación, RACS, incidentes y desempeño operativo.</p></div></div><section class="panel"><form id="dashFilters" class="filter-grid"><div class="field"><label>Unidad de negocio</label><select name="businessUnitId">${unitOptions()}</select></div><div class="field"><label>Desde</label><input type="date" name="from"></div><div class="field"><label>Hasta</label><input type="date" name="to"></div><div class="field"><label>&nbsp;</label><button class="btn primary">Aplicar filtros</button></div></form></section><div id="dashBody"></div>`;async function load(){const q=new URLSearchParams(formData($('#dashFilters')));const d=await api(`/api/dashboard?${q}`);$('#dashBody').innerHTML=`<div class="kpi-grid">${kpi('RACS',d.kpis.racs,'Base visible','navy')}${kpi('% cierre',`${d.kpis.closurePercent}%`,`${d.kpis.lifted} levantados`,'green')}${kpi('Riesgo alto',d.kpis.high,'Atención prioritaria','red')}${kpi('Trabajadores',d.kpis.workers,'Base maestra','teal')}${kpi('Temas activos',d.kpis.trainings,'Capacitaciones','amber')}${kpi('Incidentes abiertos',d.kpis.openIncidents,'Seguimiento','coral')}</div><div class="grid-2"><section class="panel"><h3>RACS por unidad</h3><div class="panel-sub">Distribución de la base filtrada</div>${bars(d.units)}</section><section class="panel"><h3>Estado operativo</h3><div class="panel-sub">Pendientes, proceso, validación y levantados</div>${bars(d.states)}</section></div><section class="panel"><h3>Lectura ejecutiva</h3><p>${d.kpis.pending} RACS requieren seguimiento, ${d.kpis.high} son de riesgo alto y ${d.kpis.openIncidents} incidentes siguen abiertos. La misma base alimenta los reportes ejecutivos.</p></section>`;}$('#dashFilters').onsubmit=e=>{e.preventDefault();load()};await load();}
