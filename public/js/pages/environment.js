import { api } from '../api.js';import { can,unitOptions,escapeHtml } from '../state.js';import { $,formData,kpi,bars,table,toast } from '../ui.js';
export async function environmentPage(root){root.innerHTML=`<div class="page-head"><div><h2>Medio ambiente</h2><p>RACS ambientales identificados por IA y registro inicial de consumo de agua. El módulo queda listo para ampliar indicadores.</p></div>${can('environment:manage')?'<button class="btn amber" id="reclassifyEnv">Reclasificar RACS con IA</button>':''}</div><section class="panel"><form id="envFilters" class="filter-grid"><div class="field"><label>Unidad</label><select name="businessUnitId">${unitOptions()}</select></div><div class="field"><label>Desde</label><input type="date" name="from"></div><div class="field"><label>Hasta</label><input type="date" name="to"></div><div class="field"><label>&nbsp;</label><button class="btn primary">Aplicar</button></div></form></section>${can('environment:manage')?`<section class="panel"><h3>Registrar indicador ambiental</h3><form id="envMetric" class="form-grid"><div class="field"><label>Unidad</label><select name="businessUnitId" required>${unitOptions()}</select></div><div class="field"><label>Fecha</label><input type="date" name="metricDate" value="${new Date().toISOString().slice(0,10)}"></div><div class="field"><label>Indicador</label><input name="metricType" value="CONSUMO DE AGUA"></div><div class="field"><label>Valor</label><input type="number" step="0.001" name="value" required></div><div class="field"><label>Unidad de medida</label><input name="unit" value="m3"></div><div class="field"><label>Meta</label><input type="number" step="0.001" name="targetValue"></div><div class="field span-2"><label>Fuente</label><input name="source"></div><div class="field"><label>&nbsp;</label><button class="btn primary">Guardar indicador</button></div></form></section>`:''}<div id="envDash"></div>`;async function load(){const q=new URLSearchParams(formData($('#envFilters')));const d=await api(`/api/environment/dashboard?${q}`);const water=d.kpis.latestWater;$('#envDash').innerHTML=`<div class="kpi-grid">${kpi('RACS ambientales',d.kpis.environmentalRacs,'Clasificados por IA','teal')}${kpi('Alto potencial',d.kpis.high,'Ambientales críticos','red')}${kpi('% cierre',`${d.kpis.closurePercent}%`,'Levantamiento','green')}${kpi('Consumo de agua',water?`${water.value} ${water.unit}`:'Pendiente',water?String(water.metric_date).slice(0,10):'Sin información','amber')}</div><div class="grid-2"><section class="panel"><h3>Categorías ambientales</h3>${bars(d.categories)}</section><section class="panel"><h3>Indicadores registrados</h3>${table(['Fecha','Unidad','Indicador','Valor','Meta','Fuente'],d.metrics.slice(0,20).map(x=>`<tr><td>${String(x.metric_date).slice(0,10)}</td><td>${escapeHtml(x.business_unit)}</td><td>${escapeHtml(x.metric_type)}</td><td>${x.value} ${escapeHtml(x.unit)}</td><td>${x.target_value??'—'}</td><td>${escapeHtml(x.source||'')}</td></tr>`))}</section></div>`;}$('#envFilters').onsubmit=e=>{e.preventDefault();load()};if($('#envMetric'))$('#envMetric').onsubmit=async e=>{e.preventDefault();try{await api('/api/environment/metrics',{method:'POST',body:formData(e.currentTarget)});toast('Indicador guardado');load();}catch(err){toast(err.message,'error')}};if($('#reclassifyEnv'))$('#reclassifyEnv').onclick=async()=>{try{const r=await api('/api/environment/reclassify-racs',{method:'POST'});toast(`${r.environmental} RACS ambientales identificados`);load();}catch(err){toast(err.message,'error')}};await load();}
