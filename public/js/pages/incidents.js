import { api,download } from '../api.js';import { state,unitOptions,areaOptions,escapeHtml } from '../state.js';import { $,formData,kpi,bars,table,toast,modal } from '../ui.js';
export async function incidentsPage(root){root.innerHTML=`<div class="page-head"><div><h2>Incidentes y accidentes</h2><p>Digitación del Flash Report, evidencias, seguimiento y análisis por fecha, ocurrencia y solución.</p></div></div><div id="incidentDash"></div><div class="grid-2"><section class="panel"><h3>Crear Flash Report</h3><form id="flashForm"><div class="form-grid two"><div class="field"><label>Tipo de evento</label><select name="eventType"><option>INCIDENTE</option><option>ACCIDENTE</option><option>CASI ACCIDENTE</option><option>DAÑO A LA PROPIEDAD</option></select></div><div class="field"><label>Severidad potencial</label><select name="potentialSeverity"><option>MEDIO</option><option>ALTO</option><option>CRITICO</option><option>BAJO</option></select></div><div class="field"><label>Fecha</label><input type="date" name="eventDate" value="${new Date().toISOString().slice(0,10)}" required></div><div class="field"><label>Hora</label><input type="time" name="eventTime"></div><div class="field"><label>Unidad</label><select name="businessUnitId" id="flashUnit" required>${unitOptions()}</select></div><div class="field"><label>Área</label><select name="areaId" id="flashArea">${areaOptions()}</select></div><div class="field"><label>Lugar</label><input name="place" required></div><div class="field"><label>Empresa</label><input name="company"></div><div class="field"><label>Involucrado</label><input name="involvedPerson"></div><div class="field"><label>Cargo</label><input name="involvedPosition"></div><div class="field"><label>Supervisor inmediato</label><input name="immediateSupervisor"></div><div class="field"><label>Grupo</label><input name="eventGroup" value="SEGURIDAD"></div><div class="field span-2"><label>Descripción del evento</label><textarea name="eventDescription" required></textarea></div><div class="field span-2"><label>Descripción del daño</label><textarea name="damageDescription"></textarea></div><div class="field span-2"><label>Acciones inmediatas</label><textarea name="immediateActions"></textarea></div><div class="field span-2"><label>Imágenes / evidencias</label><input type="file" name="images" multiple accept="image/*"></div></div><div class="actions"><button type="button" class="btn amber" id="classifyFlash">Sugerir severidad</button><button class="btn primary">Guardar Flash Report</button></div><div id="flashAi"></div></form></section><section class="panel"><h3>Filtros de seguimiento</h3><form id="incidentFilters"><div class="form-grid two"><div class="field"><label>Unidad</label><select name="businessUnitId">${unitOptions()}</select></div><div class="field"><label>Tipo</label><select name="eventType"><option value="">Todos</option><option>INCIDENTE</option><option>ACCIDENTE</option><option>CASI ACCIDENTE</option></select></div><div class="field"><label>Desde</label><input type="date" name="from"></div><div class="field"><label>Hasta</label><input type="date" name="to"></div><div class="field"><label>Estado</label><select name="status"><option value="">Todos</option><option>PENDIENTE</option><option>EN INVESTIGACION</option><option>CERRADO</option></select></div><div class="field"><label>&nbsp;</label><button class="btn primary">Aplicar</button></div></div></form></section></div><section class="panel"><h3>Flash Reports y seguimiento</h3><div id="incidentList"></div></section>`;$('#flashUnit').onchange=()=>$('#flashArea').innerHTML=areaOptions('',$('#flashUnit').value);async function load(){const [d,rows]=await Promise.all([api('/api/incidents/dashboard'),api(`/api/incidents?${new URLSearchParams(formData($('#incidentFilters')))}`)]);$('#incidentDash').innerHTML=`<div class="kpi-grid">${kpi('Eventos',d.kpis.total,'Base registrada','navy')}${kpi('Abiertos',d.kpis.open,'En seguimiento','amber')}${kpi('Cerrados',d.kpis.closed,'Solucionados','green')}${kpi('Alta severidad',d.kpis.high,'Potencial alto/crítico','red')}${kpi('% cierre',`${d.kpis.closurePercent}%`,'Seguimiento','teal')}</div><div class="grid-3"><section class="panel"><h3>Por tipo</h3>${bars(d.byType)}</section><section class="panel"><h3>Por mes</h3>${bars(d.byMonth.map(x=>({name:x.month,total:x.total})))}</section><section class="panel"><h3>Por unidad</h3>${bars(d.byUnit)}</section></div>`;$('#incidentList').innerHTML=table(['Código','Fecha','Tipo','Unidad','Lugar','Involucrado','Severidad','Estado','Acciones'],rows.map(r=>`<tr><td><b>${escapeHtml(r.report_code||r.id)}</b></td><td>${String(r.event_date).slice(0,10)} ${escapeHtml(r.event_time||'')}</td><td>${escapeHtml(r.event_type)}</td><td>${escapeHtml(r.business_unit_name||r.business_unit||'')}</td><td>${escapeHtml(r.place)}</td><td>${escapeHtml(r.involved_person||'')}</td><td><span class="tag ${['ALTO','CRITICO'].includes(r.potential_severity)?'high':'medium'}">${escapeHtml(r.potential_severity||r.severity_category||'')}</span></td><td>${escapeHtml(r.followup_status)}</td><td><button class="btn small" data-follow-flash="${r.id}">Seguimiento</button> <button class="btn small amber" data-download-flash="${r.id}">Flash Excel</button></td></tr>`));document.querySelectorAll('[data-download-flash]').forEach(b=>b.onclick=()=>download(`/api/reports/incidents/${b.dataset.downloadFlash}/flash.xlsx`,`FLASH_REPORT_${b.dataset.downloadFlash}.xlsx`).catch(e=>toast(e.message,'error')));document.querySelectorAll('[data-follow-flash]').forEach(b=>b.onclick=()=>followModal(rows.find(x=>x.id===Number(b.dataset.followFlash)),load));}$('#classifyFlash').onclick=async()=>{const text=$('#flashForm').eventDescription.value;if(!text)return toast('Escribe la descripción','error');try{const r=await api('/api/incidents/ai/classify',{method:'POST',body:{description:text}});$('#flashForm').potentialSeverity.value=r.potentialSeverity;$('#flashAi').innerHTML=`<div class="alert ok">Sugerencia: ${escapeHtml(r.potentialSeverity)} · ${escapeHtml(r.reason)}</div>`;}catch(e){toast(e.message,'error')}};$('#flashForm').onsubmit=async e=>{e.preventDefault();const form=e.currentTarget;const payload=formData(form);payload.businessUnitName=state.catalogs.units.find(x=>x.id===Number(payload.businessUnitId))?.name||'';payload.areaName=state.catalogs.areas.find(x=>x.id===Number(payload.areaId))?.name||'';const fd=new FormData();fd.append('payload',JSON.stringify(payload));[...form.images.files].forEach(file=>fd.append('images',file));try{const r=await api('/api/incidents',{method:'POST',body:fd});toast(`Flash Report ${r.report_code} creado`);form.reset();load();}catch(err){toast(err.message,'error')}};$('#incidentFilters').onsubmit=e=>{e.preventDefault();load()};await load();}
function followModal(report,reload){const box=modal(`Seguimiento ${report.report_code||report.id}`,`<form id="flashFollow"><div class="field"><label>Causa raíz</label><textarea name="rootCause">${escapeHtml(report.root_cause||'')}</textarea></div><div class="field"><label>Acciones / solución</label><textarea name="immediateActions">${escapeHtml(report.immediate_actions||report.corrective_actions||'')}</textarea></div><div class="field"><label>Resumen de solución</label><textarea name="solutionSummary">${escapeHtml(report.solution_summary||'')}</textarea></div><div class="field"><label>Estado</label><select name="followupStatus"><option ${report.followup_status==='PENDIENTE'?'selected':''}>PENDIENTE</option><option ${report.followup_status==='EN INVESTIGACION'?'selected':''}>EN INVESTIGACION</option><option ${report.followup_status==='CERRADO'?'selected':''}>CERRADO</option></select></div><button class="btn primary">Guardar seguimiento</button></form>`);box.querySelector('#flashFollow').onsubmit=async e=>{e.preventDefault();try{await api(`/api/incidents/${report.id}`,{method:'PUT',body:formData(e.currentTarget)});toast('Seguimiento guardado');box.remove();reload();}catch(err){toast(err.message,'error')}};}
