import { api,download } from '../api.js';import { state,unitOptions,escapeHtml } from '../state.js';import { $,formData,kpi,bars,table,toast } from '../ui.js';
export async function ssomaPage(root){root.innerHTML=`<div class="page-head"><div><h2>Control SSOMA como equipo</h2><p>Plan de trabajo del día siguiente, pendientes por unidad y evidencias de levantamiento.</p></div></div><div id="ssomaDash"></div><div class="grid-2"><section class="panel"><h3>Plan de trabajo</h3><form id="planForm"><div class="form-grid two"><div class="field"><label>Fecha del plan</label><input type="date" name="planDate" value="${new Date(Date.now()+86400000).toISOString().slice(0,10)}"></div><div class="field"><label>Unidad</label><select name="businessUnitId" required>${unitOptions()}</select></div><div class="field span-2"><label>Objetivo</label><textarea name="objective" required></textarea></div><div class="field span-2"><label>Actividades (una por línea)</label><textarea name="activitiesText"></textarea></div><div class="field"><label>Estado</label><select name="status"><option>PLANIFICADO</option><option>EN EJECUCION</option><option>COMPLETADO</option></select></div></div><button class="btn primary">Guardar plan</button></form></section><section class="panel"><h3>Subir evidencia SSOMA</h3><form id="ssomaEvidence"><div class="form-grid two"><div class="field"><label>Unidad</label><select name="businessUnitId" required>${unitOptions()}</select></div><div class="field"><label>Fecha</label><input type="date" name="evidenceDate" value="${new Date().toISOString().slice(0,10)}"></div><div class="field"><label>RAC relacionado (ID opcional)</label><input name="racId" type="number"></div><div class="field"><label>Título</label><input name="title" required></div><div class="field span-2"><label>Descripción</label><textarea name="description"></textarea></div><div class="field span-2"><label>Archivo</label><input type="file" name="file" required></div></div><button class="btn amber">Subir evidencia</button></form></section></div><div class="grid-2"><section class="panel"><h3>Planes recientes</h3><div id="plans"></div></section><section class="panel"><h3>Evidencias recientes</h3><div id="evidence"></div></section></div>`;async function load(){const [d,p,e]=await Promise.all([api('/api/ssoma/dashboard'),api('/api/ssoma/plans'),api('/api/ssoma/evidence')]);$('#ssomaDash').innerHTML=`<div class="kpi-grid">${kpi('Planes para mañana',d.kpis.plans_tomorrow,'Por unidad','teal')}${kpi('Planes completados',d.kpis.completed_plans,'Histórico','green')}${kpi('Evidencias 30 días',d.kpis.evidenceLast30,'Archivos SSOMA','amber')}</div><section class="panel"><h3>Pendientes por unidad</h3>${bars(d.pending)}</section>`;$('#plans').innerHTML=table(['Fecha','Unidad','SSOMA','Objetivo','Estado'],p.slice(0,20).map(x=>`<tr><td>${String(x.plan_date).slice(0,10)}</td><td>${escapeHtml(x.business_unit)}</td><td>${escapeHtml(x.ssoma_name)}</td><td>${escapeHtml(x.objective)}</td><td>${escapeHtml(x.status)}</td></tr>`));$('#evidence').innerHTML=table(['Fecha','Unidad','SSOMA','Título','RAC','Drive'],e.slice(0,20).map(x=>`<tr><td>${String(x.evidence_date).slice(0,10)}</td><td>${escapeHtml(x.business_unit)}</td><td>${escapeHtml(x.ssoma_name)}</td><td>${escapeHtml(x.title)}</td><td>${escapeHtml(x.report_code||'')}</td><td>${x.drive_web_link?`<a href="${escapeHtml(x.drive_web_link)}" target="_blank">Abrir</a>`:escapeHtml(x.drive_status||'LOCAL')}</td></tr>`));}$('#planForm').onsubmit=async e=>{e.preventDefault();const d=formData(e.currentTarget);d.activities=d.activitiesText.split('\n').map(x=>x.trim()).filter(Boolean);try{await api('/api/ssoma/plans',{method:'POST',body:d});toast('Plan guardado');load();}catch(err){toast(err.message,'error')}};$('#ssomaEvidence').onsubmit=async e=>{e.preventDefault();try{await api('/api/ssoma/evidence',{method:'POST',body:new FormData(e.currentTarget)});toast('Evidencia guardada y enviada a Drive si está configurado');e.currentTarget.reset();load();}catch(err){toast(err.message,'error')}};await load();}

export async function resourcesPage(root){root.innerHTML=`<div class="page-head"><div><h2>Descarga de recursos ejecutivos</h2><p>Un único PPT Ejecutivo oficial de RACS, estático y fiel al modelo institucional. Capacitación conserva Dashboard y Excel Ejecutivo.</p></div></div><section class="panel"><form id="resourceFilters" class="filter-grid"><div class="field"><label>Unidad</label><select name="businessUnitId">${unitOptions()}</select></div><div class="field"><label>Desde</label><input type="date" name="from"></div><div class="field"><label>Hasta</label><input type="date" name="to"></div><div class="field"><label>Estado</label><select name="status"><option value="">Todos</option>${state.catalogs.racStatuses.map(x=>`<option>${x}</option>`).join('')}</select></div></form></section><div class="grid-2"><section class="panel"><h3>RACS · Recursos ejecutivos</h3><p>Portada, resumen global, análisis por unidad, personal, RACS/trabajador, actos, condiciones, alto potencial, supervisores, áreas, riesgos, causas y tabla de levantamiento.</p><div class="actions"><button class="btn amber" id="racPpt">PPT Ejecutivo</button><button class="btn primary" id="racExcel">Excel Ejecutivo</button><button class="btn ghost" id="publicLink">Crear hipervínculo</button></div><div id="linkResult"></div></section><section class="panel"><h3>Capacitación · Recursos ejecutivos</h3><p>Cumplimiento de capacitaciones, notas, capacitados, aprobación, áreas y temas.</p><div class="actions"><button class="btn primary" id="trainingExcelResource">Excel Ejecutivo</button></div></section></div>`;const qs=()=>new URLSearchParams(formData($('#resourceFilters'))).toString();$('#racPpt').onclick=()=>download(`/api/reports/racs/executive.pptx?${qs()}`,'CAPSAN6_REPORTE_EJECUTIVO_RACS.pptx').catch(e=>toast(e.message,'error'));$('#racExcel').onclick=()=>download(`/api/reports/racs/executive.xlsx?${qs()}`,'CAPSAN6_REPORTE_EJECUTIVO_RACS.xlsx').catch(e=>toast(e.message,'error'));$('#trainingExcelResource').onclick=()=>download(`/api/reports/training/executive.xlsx?${qs()}`,'CAPSAN6_REPORTE_EJECUTIVO_CAPACITACION.xlsx').catch(e=>toast(e.message,'error'));$('#publicLink').onclick=async()=>{try{const filters=formData($('#resourceFilters'));const r=await api('/api/reports/public-link',{method:'POST',body:{scope:'RACS_EXECUTIVE',filters,hours:168}});$('#linkResult').innerHTML=`<div class="alert ok">Hipervínculo válido por 7 días.<br><input value="${escapeHtml(r.url)}" readonly style="width:100%;margin-top:8px"><button class="btn small" id="copyLink">Copiar</button></div>`;$('#copyLink').onclick=()=>navigator.clipboard.writeText(r.url).then(()=>toast('Enlace copiado'));}catch(e){toast(e.message,'error')}};}
