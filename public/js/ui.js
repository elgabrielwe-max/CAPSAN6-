import { escapeHtml } from './state.js';
export const $=(q,root=document)=>root.querySelector(q);export const $$=(q,root=document)=>[...root.querySelectorAll(q)];
export function toast(message,type='ok'){const el=document.createElement('div');el.className=`toast ${type}`;el.textContent=message;document.body.appendChild(el);setTimeout(()=>el.classList.add('show'),10);setTimeout(()=>{el.classList.remove('show');setTimeout(()=>el.remove(),250)},3500);}
export function errorBox(error){return `<div class="alert danger">${escapeHtml(error?.message||error)}</div>`;}
export function loading(text='Cargando…'){return `<div class="page-loading"><span class="spinner"></span>${escapeHtml(text)}</div>`;}
export function kpi(title,value,note='',tone='teal'){return `<article class="kpi ${tone}"><div><small>${escapeHtml(title)}</small><strong>${escapeHtml(value)}</strong><span>${escapeHtml(note)}</span></div></article>`;}
export function bars(items=[],maxItems=10){const max=Math.max(1,...items.slice(0,maxItems).map(x=>Number(x.total||x.value||0)));return `<div class="bars">${items.slice(0,maxItems).map(x=>`<div class="bar-row"><span title="${escapeHtml(x.name||x.title)}">${escapeHtml(x.name||x.title)}</span><div><i style="width:${Math.round(Number(x.total||x.value||0)*100/max)}%"></i></div><b>${escapeHtml(x.total??x.value??0)}</b></div>`).join('')||'<p class="muted">Sin datos</p>'}</div>`;}
export function table(headers,rows){return `<div class="table-wrap"><table><thead><tr>${headers.map(x=>`<th>${escapeHtml(x)}</th>`).join('')}</tr></thead><tbody>${rows.length?rows.join(''):`<tr><td colspan="${headers.length}" class="empty">Sin registros</td></tr>`}</tbody></table></div>`;}
export function formData(form){return Object.fromEntries(new FormData(form).entries());}
export function modal(title,content){let box=document.getElementById('modal');if(!box){box=document.createElement('div');box.id='modal';document.body.appendChild(box);}box.innerHTML=`<div class="modal-backdrop"><section class="modal-card"><header><h3>${escapeHtml(title)}</h3><button class="icon-btn" data-close>×</button></header><div class="modal-body">${content}</div></section></div>`;box.querySelector('[data-close]').onclick=()=>box.remove();return box;}
